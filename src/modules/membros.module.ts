//import { Module } from '@nestjs/common';
//import { MongooseModule } from '@nestjs/mongoose';
//import { MembrosService } from '../services/membros.service';
//import { MembrosController } from '../controllers/membros.controller';
//import { Membro, MembroSchema } from '../schemas/membro.schema';

//@Module({
 // imports: [MongooseModule.forFeature([{ name: Membro.name, schema: MembroSchema }])],
  //controllers: [MembrosController],
 // providers: [MembrosService],
  //exports: [MembrosService],
//})
//export class MembrosModule {}
