//import { Controller, Get, Post, Body, Param, Delete, Put } from '@nestjs/common';
//import { ScrumSquadService } from '../services/scrum-squad.service';
//import { CreateSquadDto } from '../entities/dto/create-squad.dto';
//import { UpdateSquadDto } from '../entities/dto/update-aluno.dto';

//@Controller('squads')
//export class ScrumSquadController {
  //constructor(private readonly squadService: ScrumSquadService) {}

  //@Post()
  //create(@Body() dto: CreateSquadDto) {
  //  return this.squadService.create(dto);
  //}

//  @Get()
 // findAll() {
 //   return this.squadService.findAll();
 // }

  //@Get(':id')
 // findOne(@Param('id') id: string) {
 //   return this.squadService.findOne(id);
  //}

 // @Put(':id')
  //update(@Param('id') id: string, @Body() dto: UpdateSquadDto) {
  //  return this.squadService.update(id, dto);
  //}

  //@Delete(':id')
  //remove(@Param('id') id: string) {
 //   return this.squadService.remove(id);
 // }
//}
