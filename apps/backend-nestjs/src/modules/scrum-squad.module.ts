//import { Module } from '@nestjs/common';
//import { MongooseModule } from '@nestjs/mongoose';
//import { ScrumSquadController } from '../controllers/scrum-squad.controller';
//import { ScrumSquadService } from '../services/scrum-squad.service';
//import { Squad, SquadSchema } from '../schemas/squad.schema';

//@Module({
  //imports: [
    //MongooseModule.forFeature([{ name: Squad.name, schema: SquadSchema }]),
  //],
  //controllers: [ScrumSquadController],
  //providers: [ScrumSquadService],
  //exports: [ScrumSquadService],
//})
//export class ScrumSquadModule {}
